# Test Plan

Generated by `/plan-eng-review` rerun on 2026-06-19.

Branch: `main`
Repo: `DevPathAi/prototype`

## Affected Pages/Routes

- `/prototype/` overview hero: verify the evidence-led preview replaces the four feature tiles and opens `데모 > 학습 루프`.
- `/prototype/` demo section: verify `학습 루프` is the default demo destination and `기능 탐색` contains the existing independent simulators.
- `/prototype/` guided learning loop: verify compact, medium, and wide stage layouts, context invalidation, completion evidence, mentor summary copy, and restart.

## Key Interactions To Verify

- Change an approved LCS field after downstream output exists; later output clears and the context stage shows one `새 결과 생성` recovery action.
- Navigate at widths `599`, `600`, `979`, and `980`; only `600-979` medium comparison may render two columns while other stages remain single column.
- Use keyboard navigation through every stage; focus order matches visual order and all dynamic stage, reset, copy, and error outcomes are announced once.
- Copy mentor summary successfully and through failure recovery; copied payload matches only the visible approved-context summary.

## Edge Cases

- Text scale `2.0` in widget tests: no clipped labels, hidden actions, overlap, or horizontal overflow.
- Browser zoom `200%` on the release build: content remains readable, actions reachable, and no hover-only affordances are required.
- Font asset failure: missing font file, missing license, changed hash, or failed Korean fixture glyph render must fail verification before deploy.
- Static asset budget failure: font bytes over cap or total web assets over +10% must fail CI; total assets above +5% warn.

## Critical Paths

- Hero CTA -> guided loop start -> context approval -> contextual comparison -> path delta -> employment evidence -> mentor summary copy.
- Existing simulator path: guided loop -> `기능 탐색` -> each legacy simulator -> return to guided loop with last valid stage restored.
- Release delivery: `flutter analyze`, `flutter test --coverage`, feature coverage checker, release build, JS/static asset budget checks, Pages HTTP smoke, release-browser 200% zoom QA.
